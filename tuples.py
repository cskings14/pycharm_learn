'''
Tuples are pretty much the same as lists
they use () instead of []
they are immutable meaning they can't be changed

'''

numbers = (1, 2, 3)

print(numbers[0])

numbers[0] = 10  # makes an error because tuples can't be changed







